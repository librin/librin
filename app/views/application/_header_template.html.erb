

	<%if user_signed_in? %>
		<%= form_tag('/resource/search', method: "get") do%>
		
		<%= text_field_tag :search, params[:search]%>
		<%= submit_tag(value="Buscar")%>
	<%end%>
	<%end%><br/>
	
	<%if user_signed_in? %>
		<%= link_to 'Log Out', destroy_user_session_path,
		method: :delete %>
	
	<%end%>
	

